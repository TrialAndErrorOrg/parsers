import { rejourRejourFrontmatter } from './rejour-rejour-frontmatter'

describe('rejourRejourFrontmatter', () => {
  it('should work', () => {
    expect(rejourRejourFrontmatter()).toEqual('rejour-rejour-frontmatter')
  })
})
