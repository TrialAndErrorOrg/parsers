import { processorsJoteDocxTex } from './processors-jote-docx-tex'

describe('processorsJoteDocxTex', () => {
  it('should work', () => {
    expect(processorsJoteDocxTex()).toEqual('processors-jote-docx-tex')
  })
})
