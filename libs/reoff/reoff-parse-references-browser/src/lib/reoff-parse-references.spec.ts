import reoffReoffParseReferences from './reoff-parse-references.js'

describe('reoffReoffParseReferences', () => {
  it('should work', () => {
    expect(reoffParseReferences()).toEqual('reoff-reoff-parse-references')
  })
})
