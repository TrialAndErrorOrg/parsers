import { utilsBetterCheckDependencies } from './utils-better-check-dependencies.js'

describe('utilsBetterCheckDependencies', () => {
  it('should work', () => {
    expect(utilsBetterCheckDependencies()).toEqual('utils-better-check-dependencies')
  })
})
